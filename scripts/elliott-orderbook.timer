[Unit]
Description=Run Elliott Wave Order-Book Notifier every 5 minutes

[Timer]
# Runs every 5 minutes (at :00, :05, :10, etc.) — same cadence as elliott-notifier.timer
OnCalendar=*:0/5
Persistent=true

[Install]
WantedBy=timers.target
