# Configuration for Stock Swing Trading Platform
# ─────────────────────────────────────────────────────────────

symbols:
  - symbol: "BMRI.JK"
    name: "Bank Mandiri (Persero) Tbk"
    currency: "IDR"
    timeframes:
      - "1D"

indices:
  ihsg: "^JKSE"
  financial_sector: "^JKLQ45"  # IDX LQ45 Index as liquid/financial proxy

paths:
  data_dir: "data/stock"
  predictions_db: "data/predictions.db"

telegram:
  bot_token: "8529642967:AAHTiW7xKzTagFZu5BgVbQk8UQdm2AhW0jw"
  chat_ids:
    - "1179211752"

apis:
  alpha_vantage:
    api_key: ""  # Load dynamically via env if needed
  twelve_data:
    api_key: ""  # Load dynamically via env if needed

fundamentals:
  BMRI.JK:
    pe_ratio: 10.8
    pb_ratio: 2.05
    roe: 0.195
    div_yield: 0.048
    revenue_growth: 0.10

